SECRET_JWT=changethis
SECRET_MANAGER=changethis

# Environment
PROJECT_NAME=Retarget

# Backend
BACKEND_CORS_ORIGINS=["http://localhost", "http://localhost:8080", "http://127.0.0.1:8080", "https://localhost", "https://localhost:8080", "https://127.0.0.1:8080"]

# Postgress
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=database_name_changethis
POSTGRES_USER=postgres
POSTGRES_PASSWORD=database_password_changethis
